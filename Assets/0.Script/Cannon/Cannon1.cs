using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cannon1 : Cannon
{
    // Start is called before the first frame update
    void Start()
    {
        Init(1);
    }
}
