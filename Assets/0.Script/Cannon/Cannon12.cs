using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cannon12 : Cannon
{

    // Start is called before the first frame update
    void Start()
    {
        Init(3);
        //AttPower = data.power;
    }

    // Update is called once per frame
}
